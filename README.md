# react-redux-small-boilerplate

A small boilerplate with React, Redux, Babel, sass and webpack.

- clone && cd your_folder
- npm install
- export PATH=$PATH:./node_modules/.bin/
- webpack
- webpack-dev-server
